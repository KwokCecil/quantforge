# T007 策略配置系统优化

> **状态**：completed
> **所属阶段**：Phase 4 — 系统完善
> **创建日期**：2026-05-01
> **完成日期**：2026-05-03

## 一、前置依赖

- 无硬依赖。当前 `config/strategies/` 目录已存在但空置，`StrategyConfig` 基类已有 `to_dict()`/`from_dict()` 方法。

## 二、目标

为每个策略建立 JSON 配置文件的读取/写入/切换机制，实现：
1. 策略参数从代码中移到配置文件，支持多套配置并存
2. 便捷的配置切换方式（ABC 模板）
3. 主入口脚本通过简单的参数指定使用哪套配置

## 三、实施计划

### Step 1：扩展 StrategyConfig 增加文件读写

```python
# core/config.py — StrategyConfig 新增两个方法

@classmethod
def from_json(cls, filepath: str) -> 'StrategyConfig':
    """从 JSON 文件加载配置"""
    with open(filepath, 'r', encoding='utf-8') as f:
        return cls.from_dict(json.load(f))

def to_json(self, filepath: str):
    """保存配置到 JSON 文件"""
    with open(filepath, 'w', encoding='utf-8') as f:
        json.dump(self.to_dict(), f, ensure_ascii=False, indent=2)
```

### Step 2：创建 ROC 策略的多套配置模板

```
config/strategies/roc_momentum/
├── default.json           # 当前最优参数（buy_roc_edge=20, roc_n=22）
├── conservative.json      # 保守版（buy_roc_edge=25, top_k=3）
├── aggressive.json        # 激进版（buy_roc_edge=15, top_k=10）
└── experimental.json      # 实验版（multi_factor=True, inverse_vol=True）
```

配置文件格式示例（`default.json`）：
```json
{
  "strategy_name": "roc_momentum",
  "buy_roc_edge": 20.0,
  "roc_n": 22,
  "roc_m": 8,
  "top_k": 5,
  "sell_roc_edge": 3.0,
  "sell_ma_roc_edge": 0.0,
  "HIGH_WATERMARK_STOP": true,
  "high_watermark_stop_edge": 0.10,
  "CUT_LOSS": true,
  "cut_loss_edge": 0.08,
  "STRICT_BUY": false,
  "MA_PRICE_CROSS": false,
  "ROC_MA_DIRECTION": false,
  "STOP_SMALL_TRADE": true,
  "TOP_K_SELL": false,
  ...
}
```

### Step 3：创建策略工厂函数

```python
# strategies/factory.py

def create_strategy(strategy_name: str, preset: str = "default") -> Strategy:
    """
    策略工厂——通过策略名 + 预设名创建配置好的策略实例。

    用法:
      strategy = create_strategy("roc_momentum", "conservative")
      strategy = create_strategy("roc_momentum")  # 默认 default

    配置文件路径: config/strategies/{strategy_name}/{preset}.json
    """
    ...
```

### Step 4：修改 main_backtest.py 和 main_monitor.py 支持配置选择

```python
# main_backtest.py
# 用法: .\.venv\Scripts\python.exe main_backtest.py --preset aggressive
#        .\.venv\Scripts\python.exe main_backtest.py --preset conservative

def main(preset: str = "default"):
    config = ROCConfig.from_json(f"config/strategies/roc_momentum/{preset}.json")
    ...
```

可通过简单的 `sys.argv` 解析（不引入 argparse 避免过度设计）。

### Step 5：main_monitor.py 同步支持配置切换

监视器启动时读取指定的预设配置，或默认使用 `default.json`。

## 四、验收标准

- [x] `StrategyConfig.from_json()` 能从 JSON 文件正确加载配置
- [x] `StrategyConfig.to_json()` 能正确序列化并保存
- [x] `config/strategies/roc_momentum/` 下存在 3 套配置 JSON 文件（default/保守/激进）
- [x] `main_backtest.py` 通过 `--preset` 参数可切换配置
- [x] `main_monitor.py` 通过工厂函数加载配置
- [x] 不同预设的参数有明显差异（保守版高阈值低标的，激进版低阈值高分散）
- [x] `config/strategies/roc_momentum/` 目录入版本控制（配置文件不含密钥）

## 五、相关文件

| 文件 | 类型 | 说明 |
|------|------|------|
| `core/config.py` | 修改 | 新增 from_json/to_json 方法 |
| `config/strategies/roc_momentum/default.json` | **新建** | 默认配置(买边18,top5,31标的,InvVol) |
| `config/strategies/roc_momentum/conservative.json` | **新建** | 保守版(买边22,top3,10标的,止损6%) |
| `config/strategies/roc_momentum/aggressive.json` | **新建** | 激进版(买边12,top10,31标的,止损15%) |
| `strategies/factory.py` | **新建** | 策略工厂 create_strategy/create_config |
| `main_backtest.py` | 修改 | 支持 --preset 参数 |
| `main_monitor.py` | 修改 | 通过工厂创建策略和配置 |

## 六、验收结果

### 配置对比
| 预设 | 买边阈值 | top_k | 止损 | 标的数 | MA穿越 | InvVol |
|------|---------|-------|------|--------|--------|--------|
| **default** 🏆 | 18.0 | 5 | 8% | 31 | 否 | **是** |
| conservative | 22.0 | 3 | 6% | 10 | **是** | 否 |
| aggressive | 12.0 | 10 | 15% | 31 | 否 | 否 |

- 保守版：高阈值 + 紧止损 + MA穿越保护 + 精选10标的
- 激进版：低阈值 + 分散化 + 无止损 + 不停信号过滤
